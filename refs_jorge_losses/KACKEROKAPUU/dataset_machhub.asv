
P = table2array(readtable('dataset_machhub.csv'));

xq = linspace(0,1.2,100);
vecs0 = [0.75, 1.22, 0.4, 1.75];

X = fmincon(@find_dp, vecs0, [],[],[],[], [0.2, 1.22, 0.01, 1.5], [20, 1.22, 20, 20])



function dp = find_dp(vecs)

A = vecs(1);
M2H = vecs(2);
B = vecs(3);
C = vecs(4);

dp = A*(M2H-B)^C;

end